<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>bitify url</title>
</head>
<body>
    <h1>URL Shortener Generator</h1>
    <form style="margin-left: 20px;" method="post" action="/url/">
        <label>Enter your URL</label><br>
        <input type="text" name="url" placeholder="https://example.com"/>
        <button type="submit">Generate</button>
    </form>
    <% if(locals.id){ %>
        <p>URL generated:<a href="http://localhost:8000/<%=id%>">http://localhost:8000/<%=id%></a></p>
    <%}%>

    <% if(locals.urlres){%>
        <table>
            <thead>
                <th>Serial number</th>
                <th>short url</th>
                <th>redirect url</th>
                <th>number of time visited</th>
            </thead>
            <tbody>
                <% urlres.forEach((url,index)=>{%>
                    <tr>
                    <td><%= index+1 %></td>
                    <td><%=url.shortID%></td>
                    <td><%=url.redirectUrl%></td>
                    <td><%=url.visitHistory.length%></td>
                    </tr>
                <%})%>
            </tbody>
        </table>
    <%}%>
</body>
</html>